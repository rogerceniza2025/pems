import { createWebConfig } from '@pems/config-tailwind'

export default createWebConfig({
  // Web app specific extensions can be added here
  theme: {
    extend: {
      // Add any web app specific theme extensions
    },
  },
})
